spring:
  application:
    name: consumer-lindj
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config-server
      name: consumer-lindj
      label: master
eureka:
  client:
    serviceUrl:
      defaultZone: http://192.168.1.41:8000/eureka/